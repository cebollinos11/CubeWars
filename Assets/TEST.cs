using UnityEngine;
using System.Collections;

public class TEST : MonoBehaviour {
    private GameManager blabla;
	// Use this for initialization
	void Start () {
        GameManager = new GameManager();
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
