using UnityEngine;
using System.Collections;

public class TEST : MonoBehaviour {
    public GameManager blabla;
    private GameObject *b;
	// Use this for initialization
	void Start () {
        GameManager = new GameManager();
        b =new  GameObject();
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
